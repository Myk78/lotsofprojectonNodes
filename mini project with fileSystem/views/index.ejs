<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/stylesheet/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>

    <title>Document</title>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-900">
      <div class="form p-10 text-white">
        <form action="/createpost" method="post">
          <input
            class="block px-5 py-2 bg-zinc-800 rounded-lg w-full"
            type="text"
            name="title"
            placeholder="title"
          />
          <textarea
            class="block px-5 py-2 resize-none mt-2 bg-zinc-800 rounded-md w-full"
            name="details"
            placeholder="description"
          ></textarea>
          <input
            class="bg-gray-600 px-5 py-2 mt-2 rounded-md"
            type="submit"
            value="Create Your task"
          />
        </form>
      </div>
      <div class="tasks flex items-center p-10 gap-3 flex-wrap">
        <%if(fs.length > 0 ){%> <% fs.forEach(function(file){ %>
        <div class="task w-72 px-3 py-4 rounded-md bg-zinc-800">
          <div class="text-white text-2xl trackin-tighter"><%= file%></div>
          <div class="flex justify-between mt-3">
            <a href="/task/<%= file%>" class="text-blue-500 inline-block">
              Read more ...</a
            >
            <a href="/edit/<%= file %>" class="text-zinc-500">Edit filename</a>
          </div>
        </div>
        <% }) %> <%} else{%>
        <h3 class="text-white">No task list</h3>
        <%}%>
      </div>
    </div>
  </body>
</html>
